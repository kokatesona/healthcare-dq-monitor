name: healthcare_dq
version: "1.0.0"
config-version: 2

profile: healthcare_dq

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  healthcare_dq:
    staging:
      +materialized: view
      +schema: staging
    mart:
      +materialized: table
      +schema: mart
